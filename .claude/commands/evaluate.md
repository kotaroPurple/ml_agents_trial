---
description: 学習済みモデルの評価コードを src/ml_agents_trial/evaluation/ に生成・実行する。予測 vs 実績・残差プロット・特徴量重要度を生成する。
---

`$ARGUMENTS` の形式: `[TARGET_COLUMN]`（未指定: `MedHouseVal`）

## 前提確認

`artifacts/models/comparison.json` が存在するか確認してください。
存在しない場合は「先に `/build` を実行してください」と伝えて終了。

## Step 1 — subagent によるコード生成

@evaluator を呼び出して次の指示を渡してください:
「`artifacts/models/comparison.json` と `artifacts/eda/data_summary.json` を読んで、`src/ml_agents_trial/evaluation/` に評価モジュールを実装してください。ファイル構成は機能の規模に合わせて判断してください。実装すべき公開関数:
- `plot_predictions(y_true, y_pred, output_path)` / `plot_residuals(...)` / `plot_feature_importance(...)` / `plot_model_comparison(...)`
- `evaluate_all_models(X_test, y_test) -> dict`（全 model.pkl を読んでプロットを `artifacts/models/*/plots/` に保存）
- `generate_report(comparison_path=None) -> dict`（comparison.json から best model を判定して `report_summary.json` 保存）

ターゲット列: `[TARGET_COLUMN]`。生成後に実行して各モデルのプロットを `artifacts/models/*/plots/` に保存してください。」

## Step 2 — 構造チェック

@code-reviewer を呼び出して次の指示を渡してください:
「`src/ml_agents_trial/evaluation/` 内の全 .py ファイルを構造チェックしてください」

FAIL が返ってきた場合は @evaluator に修正を依頼してから Step 3 に進んでください。

## Step 3 — ドメインレビュー

```bash
find src/ml_agents_trial/evaluation/ -name "*.py" | sort
```
で生成ファイルを確認し、各ファイルを Read して以下を確認してください:
- `matplotlib.use("Agg")` が plot 関連ファイルの先頭にあるか
- 全プロット関数が `Path` を引数に取るか

問題があれば @evaluator に修正を依頼してください。

## Step 4 — ML品質レビュー

@ml-reviewer を呼び出して次の指示を渡してください:
「`evaluation` フェーズの ML 品質レビューを実施してください。
対象: `src/ml_agents_trial/evaluation/`, `artifacts/eda/data_summary.json`, `artifacts/models/comparison.json`」

FAIL が返ってきた場合は @evaluator に修正を依頼し、Step 2 から再確認してください。

## Step 5 — 実行

```bash
.venv/bin/python src/ml_agents_trial/evaluation/report.py [TARGET_COLUMN]
```

## Step 6 — 生成アーティファクト確認

```bash
find artifacts/models -name "*.png" | sort
```

## Step 7 — git コミット

```bash
git add src/ml_agents_trial/evaluation/
STAGED=$(git diff --name-only --cached | grep 'ml_agents_trial/evaluation/' | sed 's|src/ml_agents_trial/evaluation/||')
git commit -m "feat(evaluation): generate evaluation modules

Generated by /evaluate command (target: [TARGET_COLUMN]):
$(echo "$STAGED" | sed 's/^/- /')"
```

完了後、生成されたプロット一覧・コミットハッシュを報告してください。
次のステップ: `/report` を実行してください。
