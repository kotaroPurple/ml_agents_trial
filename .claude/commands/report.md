---
description: 全 artifacts を読んで src/ml_agents_trial/presentation/builder.py を生成し、Marp スライドを組み立てて HTML に変換する。
---

## 前提確認

`artifacts/models/comparison.json` が存在するか確認してください。

## Step 1 — subagent によるコード生成

@reporter を呼び出して次の指示を渡してください:
「`artifacts/` 以下の全 JSON を読んで、`src/ml_agents_trial/presentation/` にスライドビルダーを実装してください。ファイル構成は機能の規模に合わせて判断してください。実装すべき公開関数:
- `collect_slide_data() -> dict`（全 artifacts を集約）
- `build_slides(slide_data: dict) -> Path`（テンプレートのプレースホルダーを置換して `artifacts/presentation/slides.md` を生成）

`src/ml_agents_trial/presentation/templates/base.marp.md` を参照してプレースホルダーを確認してください。生成後に実行して動作確認してください。」

## Step 2 — 構造チェック

@code-reviewer を呼び出して次の指示を渡してください:
「`src/ml_agents_trial/presentation/` 内の全 .py ファイルを構造チェックしてください」

FAIL が返ってきた場合は @reporter に修正を依頼してから Step 3 に進んでください。

## Step 3 — ドメインレビュー

```bash
find src/ml_agents_trial/presentation/ -name "*.py" | sort
```
で生成ファイルを確認し、各ファイルを Read して以下を確認してください:
- テンプレートのプレースホルダーが全て置換されているか
- 画像パスが `artifacts/presentation/images/` 相対で参照されているか

問題があれば @reporter に修正を依頼してください。

## Step 4 — builder 実行

```bash
.venv/bin/python src/ml_agents_trial/presentation/builder.py
```

## Step 5 — Marp HTML 変換

```bash
npx --yes @marp-team/marp-cli artifacts/presentation/slides.md --output artifacts/presentation/slides.html
```

## Step 6 — git コミット

```bash
git add src/ml_agents_trial/presentation/
STAGED=$(git diff --name-only --cached | grep 'ml_agents_trial/presentation/' | grep '\.py$' | sed 's|src/ml_agents_trial/presentation/||')
git commit -m "feat(presentation): generate slide builder

Generated by /report command:
$(echo "$STAGED" | sed 's/^/- /')"
```

完了後、`artifacts/presentation/slides.html` のパス・コミットハッシュを報告してください。
